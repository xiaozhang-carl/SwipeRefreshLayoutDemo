<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical">


        <com.github.rubensousa.raiflatbutton.RaiflatButton
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="5dp"
            android:onClick="RecylerViewActivity"
            android:text="RecylerViewActivity"
            android:textAllCaps="false"/>

        <com.github.rubensousa.raiflatbutton.RaiflatButton
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="5dp"
            android:onClick="PhotoViewActivity"
            android:text="PhotoViewActivity"
            android:textAllCaps="false"/>


        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="5dp"
            android:onClick="SwipeRefreshLayoutActivity"
            android:text="SwipeRefreshLayoutActivity"
            android:textAllCaps="false"/>

    </LinearLayout>
</layout>